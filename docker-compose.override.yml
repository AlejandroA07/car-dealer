services:
  api:
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  web:
    ports:
      - "5002:8080"

  db:
    ports:
      - "5432:5432"
    container_name: westcoast-postgres
