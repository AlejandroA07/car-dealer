@model westcoast_cars.web.ViewModels.Manufacturer.ManufacturerPostViewModel
<section class="page">

    <h1 class="page-title">Lägg till ny tillverkare</h1>

    <form asp-action="Create" method="post">
        <div class="form-control">
            <label asp-for="Name"></label>
            <input asp-for="Name" type="text">
            <span class="error" asp-validation-for="Name"></span>
        </div>
        <div class="form-control">
            <div class="buttons-container">
                <button class="btn btn-dark">Spara</button>
                <a asp-action="Index" asp-controller="Admin" class="btn btn-warning">Avbryt</a>
            </div>
        </div>
    </form>

    <section class="list">
            <section class="list">
        @foreach (var make in @Model.Manufacturers)
        {
            <div>
                <span>@make.Id</span>&nbsp;<span>@make.Name</span>
                <a asp-action="Delete" asp-route-id="@make.Id" class="btn btn-danger">Delete</a>
            </div>
        }
    </section>
    </section>
</section>